class GeoLocation {
  String? name;
  num? latitude;
  num? longitude;
  GeoLocation(String? name, num latitude, num longitude) {
    this.name = name;
    this.latitude = latitude;
    this.longitude = longitude;
  }
}
